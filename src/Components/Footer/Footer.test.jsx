import { describe, it, expect } from "vitest";

describe("Footer Page test", () => {
    it("would set up a test to ensure footer renders as it's currently static - same as the header", () => {
        expect(true).toBeTruthy();
    });
});
