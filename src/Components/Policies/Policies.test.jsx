import { describe, it, expect } from "vitest";

describe("Policy Page test", () => {
    it("would set up a test to ensure policy page renders as it's currently static - same as the header", () => {
        expect(true).toBeTruthy();
    });
});
